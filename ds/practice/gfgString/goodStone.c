#include<stdio.h>
void  isStone(int arr[],int size){
	for(int i=0;i<size;i++){
		while(){
		
		}
	}

}
void main(){
	int size;
	printf("Enter the size of array \n");
	scanf("%d",&size);
	int arr[size];

	for(int i=0;i<size;i++){
		scanf("%d",&size);
	}
	isStone(arr,size);

}
